## Q can do arithmetic
Let $t$ be a closed term in the language of arithmetic. Given its evaluation $n = \{\_\}_\mathbb N t$:
$$Q \vdash t = \bar n$$
Where $\bar n$ is the *numeral* $n$, i.e. $\underbrace{S...S}_n\;0$.
