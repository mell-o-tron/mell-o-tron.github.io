## Q decides atomic closed formulae

Let $\varphi$ be atomic and closed. Then:

$$\mathbb N \models \varphi \iff Q \vdash \varphi$$
$$\mathbb N \models \neg\varphi \iff Q \vdash \neg\varphi$$

Note that the closed atomic formulae are: $t_1 = t_2$, $\top$, $\bot$.
